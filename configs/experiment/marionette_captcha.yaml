# @package _global_

defaults:
  - override /data: captcha
  - override /guide: iterative_mean_field
  - override /learner: graphical
  - override /learner/optim: clipped_adamw
  - override /model: marionette_captcha

data:
  height: 80
  width: 80

learner:
  tracer:
    _target_: src.inference.tracer.AdaptiveElboTracer
    num_particles: 4

model:
  backgrounder: null

trainer:
  epochs: 200
  save_period: 200
