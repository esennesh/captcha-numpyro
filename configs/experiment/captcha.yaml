# @package _global_

defaults:
  - override /data: captcha
  - override /guide: captcha_encoder
  - override /model: captcha

learner:
  optim:
    learning_rate: 1e-5

guide:
  backgrounder: null

model:
  backgrounder: null

trainer:
  epochs: 100
