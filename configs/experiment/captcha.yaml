# @package _global_

defaults:
  - override /data: captcha
  - override /guide: auto_mean_field
  - override /learner: batchvi
  - override /model: captcha

data:
  drop_last: true
  indexed: true

learner:
  optim:
    learning_rate: 1e-5

model:
  backgrounder: null

trainer:
  epochs: 100
